import { FormInput } from './Input';
import { SelectComponent } from './Select';

// Re-export with both names for Input
export { FormInput };
export { FormInput as Input };

// Re-export SelectComponent as Select
export { SelectComponent as Select }; 